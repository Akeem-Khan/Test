# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    # defaults:
    #   run:
    #     working-directory: backend

    env:
      NODE_ENV: DEVELOPMENT
      PORT: 4000
      CLIENT_URL: http://localhost:3000
      MDB_CONNECT: mongodb+srv://Admin:TbxRpM3qMgLhElRt@ist.jcold.mongodb.net/IST?retryWrites=true&w=majority
      JWT_SECRET: #`tv@euV;z%"9KMkB:\Gq7T>]ycT9.W;
      JWT_ACCOUNT_ACTIVATION: ASKDJBEJIANEI34U9TIHVBJAEHT
      EMAIL_USER: akeemkhanx1ur4z@gmail.com
      EMAIL_PASS: tnemdavvaddezhwk
      EMAIL_FROM: noreply@mernauth.com
      JWT_RESET_PASSWORD: jawejn3494

    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - name: Install Current Dependencies
      run: npm ci
      working-directory: backend
    - name: Run Tests
      run: npm test
      working-directory: backend
